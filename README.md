# Basic webpack/babel with webpack-dev-server example

## Installation
- Clone the repo
$ git clone https://github.com/matthew-french/basic-webpack-babel.git
$ npm install

## Usage
- Run dev environment
$ npm start

- Build files ./dist
$ npm build
